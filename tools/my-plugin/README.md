# my-plugin

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build my-plugin` to build the library.

## Running unit tests

Run `nx test my-plugin` to execute the unit tests via [Jest](https://jestjs.io).

## Notes - 27-10-2024

nx g @nx/plugin:plugin clean-architecture --directory=tools/clean-architecture


nx g @nx/plugin:plugin my-plugin --directory=tools/my-plugin
nx generate @nx/plugin:generator my-generator --directory=tools/my-plugin/src/generators/my-generator
